#include "main.h"

/**
  *mul -a function that multiplies two integers
  *@a: the first integer
  *@b: the second integer
  *
  *Return: mul
  */
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
