#include "main.h"

/**
  *add - adds two numbers and return the sum
  *@a: the first integer
  *@b: second integer
  *
  *Return: sum
  */
int add(int a, int b)
{
	int sum = 0;

	sum = a + b;
	return (sum);
}
